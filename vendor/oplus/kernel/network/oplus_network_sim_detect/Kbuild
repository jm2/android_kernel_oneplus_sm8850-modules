# SPDX-License-Identifier: GPL-2.0-only
# Cross-leaf symbol dependency on oplus_network_oem_qmi (consumes
# oem_qmi_common_req) is wired via KBUILD_EXTRA_SYMBOLS in the
# sibling Makefile (proper $(abspath …) path resolution).
MODULE = oplus_network_sim_detect
$(MODULE)-objs := sim_detect.o
EXTRA_CFLAGS += -DQCOM_PLATFORM
obj-m += $(MODULE).o
